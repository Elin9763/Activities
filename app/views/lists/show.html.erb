<section class="todoapp">
    <header class="header">
        <h1><%= @list.name %></h1>
        <%= form_for([@list, @item]) do |f| %>
            <%= f.text_field :description, :class => "new-todo", :placeholder => (@item.errors.full_messages_for(:description).first || "Enter a new item") %>

            <%= f.submit :style => "display: none" %>

        <% end %>
      </header>


      <section class="main">
        <input class="toggle-all" type="checkbox">
        <label for="toggle-all">Mark all as complete</label>
        <ul class="todo-list">
            <%= render :collection => @list.items, :partial => "items/item_li", :as => :item %>
            <br>
            <%= button_to "Delete", list_path(@list), :method => "delete", :class => "destroy" %>
            <br>
            <%= link_to 'Back', lists_path(@list) %>
        </ul>
      </section>

      <footer class="footer">
      </footer>
</section>

